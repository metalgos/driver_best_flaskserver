#아래코드를 모두 복사 붙여넣기로 실행


!pip install pyqt5 lxml --upgrade

!pip install tensorboard

!pip install tensorflow
!pip install keras

!pip install imutils
#OpenCV와 Python 2.7 및 Python 3 모두에서 변환, 회전, 크기 조정, 골격화 및 Matplotlib 이미지 
#표시와 같은 기본 이미지 처리 기능을 더 쉽게 만드는 일련의 편의 기능.


!pip install cmake

!pip install pygame
!pip install pyfirmata
!pip install easydict
!pip install playsound